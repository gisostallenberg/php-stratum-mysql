/**
 * Adds two numbers
 *
 * @param p_left  The left operand.
 * @param p_right The right operand.
 *
 * @type singleton1
 *
 * @return int
 */
create procedure tst_oracle_add_procedure(p_left int, p_right int)
as
begin
  select p_right + p_left;
end
